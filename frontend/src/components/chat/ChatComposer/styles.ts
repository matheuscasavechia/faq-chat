import { cx } from '@/utils/cx'
import classes from './styles.module.css'

export const styles = {
  form: cx(classes.form),
  field: cx(classes.field),
  textarea: cx(classes.textarea),
  footer: cx(classes.footer),
  counter: cx(classes.counter),
}
