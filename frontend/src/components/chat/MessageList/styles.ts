import { cx } from '@/utils/cx'
import classes from './styles.module.css'

export const styles = {
  list: cx(classes.list),
  typing: cx(classes.typing),
  dots: cx(classes.dots),
  dot: cx(classes.dot),
  anchor: cx(classes.anchor),
}
