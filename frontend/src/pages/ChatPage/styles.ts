import { cx } from '@/utils/cx'
import classes from './styles.module.css'

export const styles = {
  page: cx(classes.page),
  intro: cx(classes.intro),
  title: cx(classes.title),
  description: cx(classes.description),
  conversationCard: cx(classes.conversationCard),
  conversation: cx(classes.conversation),
  welcome: cx(classes.welcome),
  welcomeTitle: cx(classes.welcomeTitle),
  welcomeText: cx(classes.welcomeText),
  suggestions: cx(classes.suggestions),
  composerArea: cx(classes.composerArea),
  sidebar: cx(classes.sidebar),
  categoryList: cx(classes.categoryList),
  tipList: cx(classes.tipList),
  tipItem: cx(classes.tipItem),
  tipMarker: cx(classes.tipMarker),
}
